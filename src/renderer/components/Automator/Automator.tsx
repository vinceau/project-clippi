import * as React from "react";

import { EventActions } from "./EventActions";

export const Automator: React.FC = () => {
    return (
        <div>
            <EventActions />
        </div>
    );
}